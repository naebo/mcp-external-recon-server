[project]
name = "external-recon"
version = "0.1.0"
description = "offensive external recon MCP server"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]>=1.6.0",
]